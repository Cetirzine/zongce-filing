---
name: zongce-filing
description: 综合素质测评（综测）填报与佐证材料汇编。读取学院测评细则 + 个人加分项目清单 + 佐证材料，逐项评估加分，产出「个人明细表 xlsx」与「按填报顺序编号的佐证材料汇编 docx」。触发词：综测、综合素质测评、综测加分、加分认定、佐证材料、明细表、评价体系细则。
---

# 综测填报 · zongce-filing

把「学院测评细则 + 个人加分项目清单 + 佐证材料目录」加工成两份可直接提交的成品，并保证**每一条加分都能指到证据、每一份证据都能指回条目**。

## 交付物（默认两个文件）

| 文件 | 内容 | 生成方式 |
|---|---|---|
| `<校/院>综合测评明细表（个人提交）-<姓名>.xlsx` | 所属类别 / 项目 / 加分分值，与你学院模板同构 | `scripts/build_detail_xlsx.py` |
| `综合测评佐证材料汇编-<姓名>.docx` | 标题 + 个人信息行 + `【n】一行说明` + 图片，顺序＝明细表填报顺序 | `scripts/build_evidence_docx.py` |

> 明细表的「注」通常写明"材料须按照上述顺序依次排序"，因此 **docx 的编号顺序必须等于 xlsx 的项目顺序**，两者是一对镜像。

## 输入清单（动手前先确认齐）

1. **测评细则**（PDF/Word）：各素质项的基础分、发展分、加分档次表、上限与备注。
2. **明细表模板**（xlsx）：多为**带示例行的空模板**——示例内容要整体替换，结构（类别/项目/分值）沿用。
3. **加分项目清单**（rtf/txt/md/口述）：奖项、讲座、活动、任职、志愿时长、交流项目……常附链接与"是否有佐证"备注。
4. **佐证材料目录**：图片、证书照片、公示 PDF、秩序册、名单、证明文件。
5. **个人信息**：姓名、学号、班级（仅用于表头，写文件前确认拼写）。
6. **学院下发的公示类附件**（活动参与名单、组织考评结果、班干部认定表）：既是**证据来源**，也是**口径校准器**。

## 工作流（8 步）

**Step 0 · 环境**
```bash
python3 -c "import openpyxl, docx, PIL, pymupdf; print('ok')"
```
macOS 上处理扫描件还需 `pip3 install pyobjc-framework-Vision pyobjc-framework-Quartz`；无 macOS 环境时改用 tesseract。

**Step 1 · 读细则，先建"分值-口径"表**
`pdftotext -layout 细则.pdf out.txt`（有文本层）→ 逐条抄出每个可加分项的**档位、上限、备注（是否减半/是否累加/是否取最高）**。规则速查模板见 `references/scoring-cheatsheet.md`。

**Step 2 · 读模板，分清"结构"与"示例"**
`openpyxl` 打印每格值与合并区、列宽、字体。模板里的项目名（如"校级优秀三好学生"）几乎都是示例，必须整体替换；只继承**表头、类别列、分值列、签字行、注释行**的结构与样式。

**Step 3 · 解析加分清单为条目**
每条至少记：`类型 / 名称 / 等级 / 时间 / 主办方 / 是否有佐证 / 原文备注`。原文里"无需材料""有佐证"这类备注直接决定要不要配图。

**Step 4 · 把条目对到证据**
逐条打开佐证文件核对：姓名、编号、项目名、等级、日期是否对得上。公示名单里"参与了但没签到/没勾选"要在说明里如实写。方法见 `references/evidence-toolbox.md`。

**Step 5 · 逐项定分**
对照 Step 1 的档位表。**遇到下列分叉必须问用户，不要替他决定**：
- 同一项材料可归入两个不同加分栏（如课程类比赛算"学科竞赛"还是"文化艺术比赛"）；
- 计次口径（"超过 N 次后每次计分"到底按学院公示的 N 次还是按自述的 N+1 次）；
- 同类多项是否累加（学生工作多岗、同类型荣誉）；
- 只有参与记录、没有名次记录时的档位。

**Step 6 · 生成明细表**
`scripts/build_detail_xlsx.py config.json`：脚本按 `rows` 自动推导类别/子类的合并单元格，并套用与模板一致的字体、边框、列宽。

**Step 7 · 生成佐证汇编**
`scripts/build_evidence_docx.py config.json`：每条 = `【编号】一行说明` + 若干张居中图片；图片自动 EXIF 转正、压缩到 1600px、按横竖选宽度；PDF 可用 `路径#page=N` 只渲染某一页（公示页、秩序册页）。

**Step 8 · 校验（必须做）**
- 读回 xlsx：打印全部单元格值与合并区，逐行核对分值；
- 读回 docx：打印每条说明与图片数量，核对编号连续、条目顺序与 xlsx 一致；
- 扫描两个文件的**禁写内容**（表情符号、被明确否定的字样、示例残留）；
- 检查工作区有没有 `~$xxx.xlsx` / `~$xxx.docx` 锁文件——有就说明文件正被 Office 打开，**提醒用户先关闭再重开**，否则你的写入会被回溯覆盖。

## 输出约定（默认沿用，除非用户另说）

- 顺序：**按明细表从上到下**（类别序：思想道德 → 科学文化 → 身心健康 → 人文艺术 → 劳动技能 → 附加分），证书类排在本类别内。
- **表内不写任何"小计/合计"行**（用户明确要求；包括"小计（基础分上限50分）"这类带口径说明的合计行）：每个加分项各占一行、只写自己的分值，合计留给审核人自己加。
- 按次计分的项（如"参加学术讲座，超过 4 次后每参加 1 次加 5 分"）**逐条列一行、每行照实写分值**：未触发阈值的写 `0`，触发的那次写该档分值（前 4 行 0、第 5 行 5）。
- 项目文字**尽量简洁、少加括号备注**：等级写到"三等奖"即可，不要写"（校级，本人为组长）""（2025-2026学年，无需佐证材料）"这类；只保留确有必要的一句短备注（如"（已补佐证材料）"）。
- **行高随行数走**：单行 20，两行就自动两倍（行数 × 单行高度，约 30），三行约 45；由脚本按 C 列文本视觉宽度估算，长项目名不允许被压成一行。
- 用户说"无需佐证材料"的项：**照样占一个编号**，说明里注明"（无需佐证材料）"，不配图。
- 图片不放表情符号、不加花哨水印；自制摘录图必须标注"据 <原文件名> 原文转录"。
- 文件命名带姓名与"（已填写）"，不要覆盖用户原件。

## 硬约束

- 不伪造、不拼接、不"美化"证据；扫描件看不清就重渲染高 DPI，而不是重画。
- 只读用户的原始材料目录，产物写在工作区新文件里。
- 有实质分歧的口径选择走 `ask`，让用户拍板，并把依据写进最终说明。

## 脚本与文档

```
scripts/scan_evidence.py       扫佐证目录 → 清单 + 压缩预览图
scripts/ocr_scanned_pdf.py     扫描版 PDF 逐页 OCR + 关键词定位 + 命中页渲染
scripts/build_detail_xlsx.py   JSON → 明细表 xlsx
scripts/build_evidence_docx.py JSON → 佐证汇编 docx
references/scoring-cheatsheet.md   分值口径速查（五育 + 附加分常见档位）
references/evidence-toolbox.md     取证工具箱（PDF/OCR/图片/docx/公示页）
references/output-conventions.md   输出格式与踩坑清单
```
